name: CI

on:
  pull_request:
  push:
    branches-ignore: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      # Includes the infrastructure tests, which assert that sign-up stays closed,
      # every route stays behind the authorizer, and the buckets stay private.
      - run: npm test
      - run: npm run build
